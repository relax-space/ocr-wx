## 为了方便您后期回顾、代码归档或提交给其他开发人员，我为您将之前所有关于 微信支付账单图片高并发 OCR 识别与多账号智能路由系统 的核心需求，整理成了如下标准的业务与技术需求规格说明书：

## 📊 一、 基础业务功能需求

1.  多格式账单深度解析与清洗

- 系统需支持通过 OCR 自动识别微信支付截图中的核心财务字段。
  - 核心字段提取矩阵：包括 采购日期、付款状态、收款官方、付款金额、支付日期、支付时间、付款方式、交易单号、商户全称、商户单号、系统记账日期、采购对账单号。
  - 同时输出每个字段的识别 置信度（Confidence Score），不为 1.0 的置信度需单独在 Excel 列中标记输出。

2.  多层级目录自动穿透与矩阵导出

- 系统需支持指定一个“图片主文件夹”，全自动递归穿透检索其下的所有子文件夹，批量提取所有常见格式的图片（.jpg, .jpeg, .png）。
  - 按文件夹物理隔离为单位导出数据：每个包含图片的文件夹内部，需自动生成一份名为 [子文件夹名]批量账单.xlsx 的 Excel 矩阵报表。
  - 自动精简列：若整张报表中某一列的“置信度”全部为空或 nan，系统需在导出时全自动剔除该无用列。

---

## 🧭 二、 智能 API 路由与熔断需求（核心升级）

1.  多接口集中式配置文件管理

- 需将支持的腾讯云 API 接口列表、内部调用 Action 以及并发限制提取到独立的外部 JSON 配置文件（ocr_api_config.json）中进行持久化保存，允许后期无需修改源码直接增减或微调。
  - 当前约定的 6 组 API 限制矩阵如下：
  - 通用文字识别（高精度版） (GeneralAccurateOCR) → 限制并发 10 次/秒
    - 表格识别（V3） (RecognizeTableAccurateOCR) → 限制并发 2 次/秒
    - 商户门头照识别 (RecognizeStoreName) → 限制并发 1 次/秒
    - 商户照片分类 (ClassifyStoreName) → 限制并发 1 次/秒
    - 广告文字识别 (AdvertiseOCR) → 限制并发 20 次/秒
    - 通用文字识别Agent (RecognizeAgent) → 限制并发 1 次/秒
  2. 泛化调用（Common Client）与域名锁定
- 底层网络物理调用摒弃原先固定的强类型 Request 结构体（如 GeneralBasicOCRRequest），全面改用腾讯云标准的泛化客户端（Common Client）形式。
  - 必须严格锁定请求域名为：://tencentcloudapi.com，不得随意更改或缺省。
  - 底层需能够根据当前激活的 API Action 名称，动态组装请求体，并根据配置文件实时调整异步高并发信号量（asyncio.Semaphore）的限流阈值。

3.  双色看板与手动指定切换

- UI 界面中央需开辟一个 API 智能路由看板，完全只显示中文描述（不展示底层英文 Action 变量）。
  - 最初状态：所有可用的 API 中文文字和单选框全部初始化渲染为 绿色。
  - 用户在启动对账前，可以通过单选框，手动自由指定当前批量任务优先使用哪一个绿色可用的接口去进行调用。

4.  自动熔断、红标锁定与级联切流

- 熔断机制：在批量对账过程中，若某个接口收到腾讯云端回执的额度枯竭报错（如 QuotaExceeded、ResourceUnavailable、UnauthorizedOperation），后台需立刻无缝将该接口的本地额度归零。
  - 红标锁定：一旦接口额度在本地账本中归零，UI 界面对应的中文单选按钮必须立即变为红色，并且强制置为禁用状态（DISABLED），使用户在界面上无法再勾选此项。
  - 级联切流：若当前正在主跑的接口（包括用户手动指定的接口）意外熔断变红，系统必须立刻在后台自动向下寻找下一个依然处于绿色状态的可用接口无缝接管后续的图片，整个切流过程不得导致当前批量并发任务中断或报错退出。

---

## 👤 三、 多账号统筹管理需求

1.  多账号凭证整块复制与两两配对

- UI 配置面板需提供一个大型多行文本框，支持用户直接将多组微信消息中传输的密钥整块复制粘贴进来。
  - 系统需具备强健的正则清洗和分流器，支持中文/英文冒号（:, ：），自动将上下文紧邻的 SecretId 和 SecretKey 两两自动成对提取，并安全保存至本地加密账本中。

2.  多账户级联轮询机制

- 当第一个账号的所有 6 个 API 接口额度全部消耗殆尽（全部变红熔断）后，系统必须自动无缝递增切换到第二个可用账号，并重新将路由看板中的所有接口恢复为绿色可用状态，继续未完的任务。

3.  顶部统筹状态条显示

- 界面最上方必须高亮常驻显示一句直观的账户池概况，格式严格为：一共 _ 个账号 ｜ 当前正使用第 _ 个账号，在后台账号自动轮询递增时，该文字处的当前账号索引需动态、实时刷新。

4.  月度全自动归位重置

- 系统在每日/每次启动或触发识别时，需自动进行跨月时间戳校验。
  - 每月 1 号（或检测到跨月时），本地账本中所有账号下的所有 API 接口额度全自动全量恢复初始额度，状态全部变回绿色可用，且任务重新强制归位，又从第一个账号的第一个绿色接口开始重新轮询调用。

---

## 🎨 四、 界面优化（瘦身需求）

1.  移除用量体检功能

- 彻底去掉系统早期版本中的“探测多账号额度健康度”按钮、用量汇总标签以及相关的冗余体检框架布局。
  - 压缩主窗体高度，将界面核心聚焦于顶部的【多账号池统筹状态条】、中央的【红绿单选路由控制中心】以及底部的【高并发执行进度条】。

2.  高颜值弹窗交互

- 移除所有系统自带的丑陋弹出框，全局通用一套精心精密计算居中、色调与状态匹配（成功用绿色，警告用黄色，磁盘或导入中断用红色）的高颜值模态弹窗（Toplevel），并完美包裹任务结束后的“📂 一键打开文件夹”等回调动作。

---

这份关于 OCR 识别与智能路由 的完整需求整理是否准确无误？

- 如果没有问题，您可以将它作为后续开发或维护的终极标准文档。
- 您国内的 Gitee 镜像下载通道 目前是否已经帮您把整合好的 main_win_all.py 成功拉取到本地了？如果需要，我们可以直接开始验证运行！
